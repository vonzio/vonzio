name: ci

# Validate every PR. No separate push:[main] run: merges are squash-only, so the
# post-merge main tree is byte-identical to the PR that just passed — re-running
# the full suite (incl. ~10min image build+scan ×2) on the merge was pure
# duplication. Releases are validated independently on v* tags (release.yml).
on:
  pull_request:
    branches: [main]

# Cancel older runs of the same PR when a new push arrives.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Runner selection is configurable: set the repository/org variable
# CI_RUNNER_LABELS (e.g. "self-hosted" or a custom label) to route CI to your
# own runners; unset, it defaults to GitHub-hosted ubuntu-latest. Lets a
# self-hoster (or the SaaS superset) move CI off metered minutes without
# editing this file per-job.
jobs:
  # Cheap gate job: detect whether anything agent-image-related changed. The
  # agent-base image (chromium + Trivy scan, ~10-20min) is independent of the
  # app/dashboard/plugin code, so on a PR that doesn't touch it we skip the
  # expensive build+scan steps below. The matrix leg still RUNS (so the required
  # check "image build+scan (agent-base)" reports green) — it just no-ops in ~20s.
  changes:
    name: changes
    runs-on: ${{ vars.CI_RUNNER_LABELS || 'ubuntu-latest' }}
    timeout-minutes: 5
    # paths-filter calls the PR list-files API. Repos with restricted
    # default GITHUB_TOKEN permissions (the private cloud mirror) fail
    # with "Resource not accessible by integration" without this —
    # which also silently disabled image build+scan on Docker-touching
    # PRs there, since the filter died before it could say "rebuild".
    permissions:
      contents: read
      pull-requests: read
    outputs:
      agent: ${{ steps.filter.outputs.agent }}
    steps:
      - uses: actions/checkout@v6
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            agent:
              - 'docker/Dockerfile.agent.base'
              - 'docker/Dockerfile.agent'
              - 'agent-runner/**'
              - '.github/workflows/ci.yml'

  typecheck:
    name: typecheck
    runs-on: ${{ vars.CI_RUNNER_LABELS || 'ubuntu-latest' }}
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm
      - name: Lockfile in sync with package.json
        # Catch lock drift (which only surfaced at release.yml's `npm ci`)
        # without a full install — see scripts/check-lockfile.sh for why `npm ci`
        # isn't usable here (npm bug #4828 / @tailwindcss/oxide native binding).
        run: bash scripts/check-lockfile.sh
      - run: npm install
      - run: npx tsc --project packages/shared/tsconfig.json --noEmit
      - run: npx tsc --project packages/plugin-api/tsconfig.json --noEmit
      - run: npx tsc --project packages/plugins/telegram/tsconfig.json --noEmit
      - run: npx tsc --project packages/plugins/slack/tsconfig.json --noEmit
      - run: npx tsc --project packages/core-server/tsconfig.json --noEmit
      - run: npx tsc --project packages/dashboard/tsconfig.json --noEmit
      # Plugins must route outbound HTTP through the audited ctx.http.fetch (§10).
      - name: no raw fetch in plugin backends
        run: node scripts/no-raw-fetch-in-plugins.mjs

  test:
    name: test
    runs-on: ${{ vars.CI_RUNNER_LABELS || 'ubuntu-latest' }}
    timeout-minutes: 15
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_DB: vonzio_test
          POSTGRES_USER: vonzio
          POSTGRES_PASSWORD: vonzio_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U vonzio"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
    env:
      # createTestDB() reads TEST_DATABASE_URL (NOT DATABASE_URL — that's
      # for the server's own runtime). Non-DB tests don't care.
      TEST_DATABASE_URL: postgres://vonzio:vonzio_test@localhost:5432/vonzio_test
      ENCRYPTION_KEY: ci-test-encryption-key-32chars-xx
      BETTER_AUTH_SECRET: ci-test-better-auth-secret-32-chars
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm
      - run: npm install
      - name: Run unit tests (DB-independent)
        # v0.1.0 limitation: createTestDB() in db/test-utils.ts does
        # DROP SCHEMA public CASCADE before running vonzio's Drizzle
        # migrations, which wipes the Better Auth tables (user, session,
        # account, verification) that migration 9 needs to ALTER. The
        # OSS install solved this by running Better Auth migrate in the
        # server container's startup wrapper; tests bypass that path.
        #
        # Proper fix: bootstrap Better Auth + Drizzle ONCE per CI run,
        # then have tests TRUNCATE data tables instead of dropping the
        # schema. Tracked as a v0.2 follow-up.
        #
        # For now CI runs the DB-independent unit tests, which is the
        # subset that's stable. Local dev runs the full suite against
        # a hand-managed postgres + better-auth-migrate.
        run: |
          npx vitest run \
            packages/shared \
            packages/plugin-api \
            packages/plugins/telegram/src/routes/stranger-cta.test.ts \
            packages/core-server/src/orchestrator/retry.test.ts \
            packages/core-server/src/orchestrator/agent-comms.test.ts \
            packages/core-server/src/container/session-registry-sweep.test.ts \
            packages/core-server/src/container/ensure-running.test.ts \
            packages/core-server/src/queue/queue.test.ts \
            packages/core-server/src/services/format-message.test.ts \
            packages/core-server/src/services/agent-output-rewriter.test.ts \
            packages/core-server/src/services/telegram-service.test.ts \
            packages/core-server/src/services/scope.test.ts \
            packages/core-server/src/services/title-service.test.ts \
            packages/core-server/src/lib/safe-webhook-fetch.test.ts \
            packages/core-server/src/mcp/mail-mcp.test.ts \
            packages/core-server/src/services/mail-service.test.ts \
            packages/core-server/src/services/calendar-service.test.ts \
            packages/core-server/src/orchestrator/chain-runner.test.ts \
            packages/core-server/src/plugins/loader.test.ts \
            packages/core-server/src/plugins/policy.test.ts \
            packages/core-server/src/plugins/membrane.test.ts \
            packages/core-server/src/plugins/scoped-db.test.ts \
            packages/core-server/src/plugins/http.test.ts \
            packages/core-server/src/plugins/audit.test.ts \
            packages/core-server/src/plugins/cli.test.ts \
            packages/core-server/src/plugins/builtins.test.ts \
            packages/core-server/src/plugins/attack-corpus.test.ts \
            packages/core-server/src/plugins/parity-check.test.ts \
            packages/core-server/src/dashboard-csp.test.ts \
            packages/dashboard

  image-build-and-scan:
    # Builds the agent-base + server images on every PR and scans both
    # with Trivy. Unified via matrix so the build/scan recipe lives in
    # one place; cache scopes are explicit per-image to keep the GHA
    # cache namespaces clean. Catches three classes of regression:
    # 1. Dockerfile breakage before it hits end-user installs (PR #66
    #    npm-ci lockfile bug would have surfaced here).
    # 2. CVEs in OS layer or bundled CLIs (git, gh, jq, chromium, etc.).
    # 3. CVEs introduced by new app dependencies.
    name: image build+scan (${{ matrix.image.name }})
    needs: changes
    runs-on: ${{ vars.CI_RUNNER_LABELS || 'ubuntu-latest' }}
    timeout-minutes: 20
    strategy:
      # Run both even if one fails -- we want to see all findings.
      fail-fast: false
      matrix:
        image:
          - name: agent-base
            file: docker/Dockerfile.agent.base
            tag: ghcr.io/vonzio/vonzio/agent-base:ci
          - name: server
            file: docker/Dockerfile.server
            tag: vonzio-server:ci
    steps:
      - uses: actions/checkout@v6
      - uses: docker/setup-buildx-action@v3
      # Daily apt cache-bust. The Dockerfiles run `apt-get upgrade`, but a
      # byte-identical RUN layer is served from the buildx cache forever, so a
      # newer debian-security patch (e.g. a chromium HIGH-CVE fix already in
      # bookworm-security) never lands and Trivy fails on an already-fixed CVE.
      # Stamping the build with the date busts the apt layers once per day so
      # the scan builds a freshly-patched image. Matches the daily-keyed Trivy
      # DB cache below. (Consumed by Dockerfile.agent.base's ARG APT_SNAPSHOT;
      # the server image ignores the unused arg.)
      - id: aptsnap
        run: echo "date=$(date -u +%Y-%m-%d)" >> "$GITHUB_OUTPUT"
      - name: Build ${{ matrix.image.name }}
        # Skip the heavy agent-base build when no agent-image source changed.
        # The server image always builds (it bundles dashboard + plugins).
        if: matrix.image.name != 'agent-base' || needs.changes.outputs.agent == 'true'
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.image.file }}
          push: false
          load: true
          tags: ${{ matrix.image.tag }}
          build-args: |
            APT_SNAPSHOT=${{ steps.aptsnap.outputs.date }}
          # Per-image cache scope: prevents cross-image cache thrash
          # and keeps any future buildx job (release, etc.) from
          # clobbering either entry by accident.
          cache-from: type=gha,scope=${{ matrix.image.name }}
          cache-to: type=gha,scope=${{ matrix.image.name }},mode=max
      - name: Scan ${{ matrix.image.name }} with Trivy
        if: matrix.image.name != 'agent-base' || needs.changes.outputs.agent == 'true'
        # Blocking gate as of v0.2: fails the job on any HIGH/CRITICAL
        # fixable CVE in OS or library layers. The Dockerfiles run
        # apt-get upgrade + npm install -g npm@latest on every rebuild
        # and the server image prunes devDeps (+ surgical delete of
        # extraneous esbuild orphans) to keep the baseline clean.
        # When a new HIGH/CRITICAL surfaces:
        #  - apt-side: usually clears on the next rebuild (apt-get
        #    upgrade pulls the latest debian-security patch)
        #  - npm-side: bump the offending dep's range in package.json
        #    until npm install resolves to a patched version
        #  - binary inside an npm package (esbuild, etc.): inspect with
        #    `npm explain`, decide whether to delete post-prune or
        #    bump the package that ships it
        # ignore-unfixed avoids failing on CVEs with no patch available
        # -- we can't act on those at PR time.
        #
        # Scoped REPORT-ONLY suppressions live in docker/trivy-ignore.rego:
        # chromium* (continuous browser CVE stream, unpatched window until
        # bookworm-security lands) and CVE-2026-42504 (Go-stdlib DoS in the
        # bundled `gh` binary, unfixable until GitHub rebuilds gh against the
        # patched Go). Both are off the exit-code gate but TRIVY_SHOW_SUPPRESSED
        # keeps them printed so regressions stay visible. See the rego for the
        # per-entry justification. Every other OS/library HIGH/CRITICAL still
        # fails the build.
        env:
          TRIVY_IGNORE_POLICY: docker/trivy-ignore.rego
          TRIVY_SHOW_SUPPRESSED: "true"
        uses: aquasecurity/trivy-action@0.35.0
        with:
          image-ref: ${{ matrix.image.tag }}
          format: table
          severity: CRITICAL,HIGH
          exit-code: "1"
          ignore-unfixed: true
          vuln-type: os,library
          # cache: true reuses the trivy vuln DB across the two
          # matrix legs and across runs -- saves ~700 MB download per leg.
          cache: true

  secret-scan:
    # Scan PR diffs for accidentally-committed secrets (API keys,
    # private keys, tokens). Catches the foot-gun of pasting a working
    # secret into a comment, test fixture, or example .env.
    #
    # Using TruffleHog instead of gitleaks-action -- gitleaks-action@v2
    # requires a paid license for GitHub orgs (vonzio is an org).
    # TruffleHog stays Apache-2.0 and free for any account type, and
    # verifies credentials against the source provider where possible
    # to cut false positives.
    name: secret scan
    runs-on: ${{ vars.CI_RUNNER_LABELS || 'ubuntu-latest' }}
    timeout-minutes: 5
    # Pull requests only -- a push-to-main scan would just confirm
    # what we already merged. Branch protection prevents direct pushes
    # to main, so any secret has to flow through a PR scanned here.
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: trufflesecurity/trufflehog@v3.96.0
        with:
          # Scan only the PR diff (base..head) -- a full-history scan
          # on every PR would be wasteful and produce repeated noise
          # for any pre-existing match.
          base: ${{ github.event.pull_request.base.sha }}
          head: ${{ github.event.pull_request.head.sha }}
          # `verified,unknown` rather than `--only-verified`:
          # - verified  = credential the provider confirmed live
          # - unknown   = credential TruffleHog matched a high-confidence
          #               pattern for but has no verifier endpoint
          #               (SSH/PGP private keys, generic JWTs, custom
          #               service tokens, .env API keys for self-hosted
          #               services). This is the class --only-verified
          #               would silently miss -- exactly the foot-gun
          #               the SECURITY_MODEL.md bullet promises to catch.
          # Drops `unverified` (long-tail high-entropy false positives).
          extra_args: --results=verified,unknown

  install-script:
    # Lint install.sh (shellcheck) AND unit-test it (bats) so we catch shell
    # footguns + dep-handling regressions before community users hit them.
    name: install.sh lint + test
    runs-on: ${{ vars.CI_RUNNER_LABELS || 'ubuntu-latest' }}
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
      - name: ShellCheck
        uses: ludeeus/action-shellcheck@2.0.0
        with:
          severity: warning
          scandir: "./"
          additional_files: "install.sh scripts/start-dev.sh"
      # macOS ships bash 3.2 and `curl | bash` runs under it, but shellcheck +
      # CI run bash 5 — so a bash-4-ism (${var,,}, declare -A, …) passes lint
      # and only breaks on a real Mac install. This greps for them. See
      # scripts/check-bash-portability.sh.
      - name: bash 3.2 portability (install.sh + scripts)
        run: bash scripts/check-bash-portability.sh
      - name: Install bats
        run: sudo apt-get update -y && sudo apt-get install -y bats
      - name: Unit-test install.sh
        run: bats test/install.bats
      # End-to-end run under the script's own `set -e` — the thing bats can't
      # see (its setup() relaxes set -e) and the release E2E gate can't see (it
      # boots via docker compose, not install.sh). Runs in-clone (this checkout)
      # with a FRESH .env, so it exercises setup_env/preflight/etc. the way a
      # real `curl | bash` does. --no-start stops before docker. This is the
      # guard that would have caught the v0.3.6 `set -e` regression.
      # No setup-node: the docker install path doesn't need host node (it's an
      # optional prereq now), so the guard shouldn't depend on it either.
      - name: install.sh end-to-end under set -e (--no-start, in-clone)
        run: VONZIO_NO_OPEN=1 bash install.sh --no-start --yes
